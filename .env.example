# Workspace-specific environment templates now live in `extension/.env.example`
# and `mobile/.env.example`. Copy the file that matches the workspace into
# `.env` and fill in your Firebase and Gemini credentials there.

# This root file is kept as a reminder not to commit secrets.