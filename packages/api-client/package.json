{
  "name": "@my-extension/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint .",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@apollo/client": "^3.8.3",
    "@my-extension/ai": "workspace:*",
    "graphql": "^16.8.1",
    "jwt-decode": "^3.1.2",
    "zustand": "^4.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.6.0",
    "eslint": "^8.49.0",
    "typescript": "^5.2.2",
    "tsup": "^7.2.0"
  }
}